"""Console package: a three-tab control plane over this repo's CLIs.

`Generator mode` seeds and exports, `Operational mode` drives the fixture's own mock
API (server join, account token file, load runs), and `Settings` holds the tool
defaults both read. Run it with `python3 -m console`.
"""
